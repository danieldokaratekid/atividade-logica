#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "portuguese");
    float salario;
    float menorsalario;
    float maiorsalario;
    float mediasalario;
    float mediafilhos;
    int quantidadefilhos;
    int filhos;
    int opcao;
    int idade;
    do
    {
        system("cls||clear");
        printf("-------Pesquisa--------\n");
        printf("---------1-Adicionar Familia-------\n");
        printf("-------2-Sair e Exibir Resultados------\n");

        switch (opcao);
    case 1:
            
        printf("Digite o seu salário:");
        scanf("%d", &salario);
        printf("Digite a quantidade de filhos que possui:");
        scanf("%f", &quantidadefilhos);
    case 2:
        mediasalario += salario / 2;
        mediafilhos += filhos / 2;
        menorsalario = maiorsalario < menorsalario ? idade : maiorsalario;
        maiorsalario = menorsalario > maiorsalario ? idade : menorsalario;
        system(cls || clear);
        printf("===Pesquisa===\n");
        printf("maior salario:%d\n", maiorsalario);
        printf("menor salario:%d\n", menorsalario);
        printf("media salarios:R$%.2f\n", mediasalario);
        printf("media filhos:%.2f\n", mediafilhos);
        while}
            (opcao!= 2);


        printf("Numero de filhos:");
        printf("maiorsalario:");
        printf("menorsalario:");
        printf("mediasalario:");
        printf("mediafilhos:");





        return 0;
    }
